import 'package:flutter/material.dart';

import '../../tabs/z_gnav_bar.dart';

class HomeView extends StatelessWidget {
  const HomeView({super.key});

  @override
  Widget build(BuildContext context) {
    return const GNavBar();
  }
}
